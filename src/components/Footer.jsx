import React from 'react';

export default function Footer() {
  return (
    <footer className="max-w-3xl mx-auto px-4 py-10 text-center text-xs text-slate-400">
      Demo prototype by Joy Ghosh — not a real booking system, no real payments processed.
    </footer>
  );
}